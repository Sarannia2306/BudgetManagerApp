<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color for EditText -->
    <corners android:radius="16dp"/> <!-- Rounded corners with 16dp radius -->
    <stroke android:width="2dp" android:color="@android:color/darker_gray"/> <!-- Border color and width -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
</shape>
