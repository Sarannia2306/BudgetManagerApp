<!-- res/drawable/edit_text_bg.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color of EditText -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
    <stroke
        android:width="1dp"
        android:color="@color/primary_blue" /> <!-- Border color -->
</shape>
